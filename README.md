# Evil Scheme

maybe-embeddable toy sorta-scheme to learn PLT

* doesn't work properly
* the evaluator is dumb, recursive, and overflows the host stack
* lists are copied pass-by-value arrays instead of linked-list cons cells
* deliberately limited features (bare minimum) cos im lazy and this isn't professional
